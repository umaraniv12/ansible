---
- hosts: linux
  vars:
    test: Welcome to ansible training
    Test: testing
  tasks:
  - name: print variable
    debug: msg="{{ test }}"
  - name: print another variable
    debug: msg="{{ Test }}"
